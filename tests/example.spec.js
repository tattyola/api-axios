import {expect} from "chai";

describe('', function () {
    it('should ', function () {
        const a = 3;
        const b = 4;
        const result = a + b;
        expect(result, 'result should be equal 7').to.equal(7);

    });
})
